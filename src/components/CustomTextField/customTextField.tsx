import { TextField, Box } from "@mui/material"

const CustomTextField = (props: any) => {
  return (
    <Box>
      <TextField
        {...props}
      />
    </Box>
  )
}

export default CustomTextField
